server:
  port: 8080

spring:
  application:
    name: travel-memory-server
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/travel_memory?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: travel_user
    password: Travel@123456
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 55MB

mybatis-plus:
  type-aliases-package: com.travelmemory.entity
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

app:
  upload:
    dir: ../uploads
    cleanup:
      enabled: false
      orphan-retention-hours: 24

map:
  reverse-geocode:
    key: ${MAP_REVERSE_GEOCODE_KEY:}

amap:
  web:
    key: ${AMAP_WEB_KEY:}
